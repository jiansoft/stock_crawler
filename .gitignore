# Rust build artifacts
/target/
/debug/
/release/

# Rustfmt / rust-analyzer backups
**/*.rs.bk

# Cargo lockfile
# 本專案為 binary 應用程式，依 Rust 慣例將 Cargo.lock 納入版控，
# 確保 CI 與正式環境相依版本可重現（避免 transitive 套件浮到不相容的新版本）。

# Local runtime artifacts
/log/
/*.log

# IDE
/.idea/

# Local configuration / secrets
.env
.env.*

# Local SSL assets
/etc/ssl/
